- name: AutoPlay Demo Infrastructure Workflow
  hosts: demo_servers
  become: yes
  tasks:
    - name: Write managed file "/tmp/autoplay-managed.txt"
      ansible.builtin.copy:
        dest: "/tmp/autoplay-managed.txt"
        mode: "0644"
        content: |
          Managed by AutoPlay

