name: AutoPlay CI/CD Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Syntax Linter
      run: |
        # Run syntax check / byte compilation checks
        python -m py_compile app/*.py tests/*.py

    - name: Run Unit Tests
      run: |
        python -m unittest discover -s tests

  build-and-deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build Docker Image
      run: |
        docker build -t autoplay-app:latest .

    # In a real environment, uncomment steps below to log in and push to Docker Registry
    # - name: Log in to Docker Hub
    #   uses: docker/login-action@v3
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}
    #
    # - name: Push Docker Image
    #   run: |
    #     docker tag autoplay-app:latest ${{ secrets.DOCKER_USERNAME }}/autoplay-app:latest
    #     docker push ${{ secrets.DOCKER_USERNAME }}/autoplay-app:latest

    # - name: Deploy to AWS EC2 via SSH / Ansible
    #   env:
    #     PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
    #     HOST: ${{ secrets.EC2_HOST }}
    #   run: |
    #     echo "$PRIVATE_KEY" > key.pem
    #     chmod 600 key.pem
    #     # Run ansible deployment playbook remotely
    #     # ansible-playbook -i ansible-deploy/inventory.ini ansible-deploy/deploy_autoplay.yml
