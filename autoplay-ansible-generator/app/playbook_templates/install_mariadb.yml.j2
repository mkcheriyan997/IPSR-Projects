    - name: Install MariaDB server
      ansible.builtin.package:
        name: mariadb-server
        state: present

    - name: Manage MariaDB service
      ansible.builtin.service:
        name: mariadb
        state: {{ mariadb_service_state | default('started') }}
        enabled: {{ mariadb_service_enabled | default('yes') }}

