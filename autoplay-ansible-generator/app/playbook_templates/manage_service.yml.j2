    - name: Manage service "{{ service_name }}"
      ansible.builtin.service:
        name: "{{ service_name }}"
        state: {{ service_state | default('started') }}
        enabled: {{ service_enabled | default('yes') }}

